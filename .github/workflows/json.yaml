name: JSON
on:
  workflow_dispatch:
    inputs:
      json:
        required: true
        type: string
jobs:
  job:
    strategy:
      matrix:
        value: ${{ fromJSON(inputs.json) }}
    runs-on: ubuntu-latest
    steps:
      - run: echo matrix.value
